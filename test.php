<?php
  $users = array('Johnson', 'james', 'bill', 'jeffrey');

    foreach($users as $user) {  
     if($_GE['name'] == 'bill') {
        unset($user);

    }
}




?>

<h3>2. <?php
function myfunction($v)
{
    return  str_replace("J", "", $v);
  
}


?></h3>

<?php $_GE['name'] = 

<h3>3. If a users name is provided as a 'get' variable we want it to be removed from the list. ie. 'index.php?name=bill' should remove 'bill' from the array</h3>

<h3>4. Write a class that accepts the user array in the constructor and saves it as a private variable. Then write a method called "getAlphabetized" that returns the alphabetized array.</h3>